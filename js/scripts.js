function Dice(currentRole){
  this.currentRole = currentRole;
}

Math.floor(Math.random(1,6));

function Score(currentScore, totalScore){
  this.currentScore = currentScore;
  this.totalScore = totalScore;
}